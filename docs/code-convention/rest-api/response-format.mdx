---
sidebar_position: 2
---

# Response Format

While working on REST API, there is such a need to have some kind of format for some kind of request. By having a response format, the response of the request can be handled easily since we knew that the response will have the expected object structures.

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs groupId="response-format">
  <TabItem value="create" label="Create a Resource" default>

```bash title="Create/Update/Get Example"
{
  "code": 200,
  "status": "OK",
  "data": {
    "id": 1,
    "username": "krsbx"
  }
}
```

```bash title="Create/Update/Get JSON Schema"
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "code": {
      "type": "integer"
    },
    "status": {
      "type": "string"
    },
    "data": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "required": ["code", "status", "data"]
}
```

  </TabItem>

  <TabItem value="gets" label="Get Multiple Resource">

```bash title="Get Multiple Resource Example"
{
  "code": 200,
  "status": "OK",
  "data": [
    {
      "id": 1,
      "username": "krsbx"
    }
  ],
  "page": {
    "size": 1,
    "total": 1,
    "totalPages": 1,
    "current": 1
  }
}
```

```bash title="Get Multiple Resource JSON Schema"
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "code": {
      "type": "integer"
    },
    "status": {
      "type": "string"
    },
    "data": {
      "type": "array",
      "items": [
        {
          "type": "object",
          "properties": {},
          "required": []
        }
      ]
    },
    "page": {
      "type": "object",
      "properties": {
        "size": {
          "type": "integer"
        },
        "total": {
          "type": "integer"
        },
        "totalPages": {
          "type": "integer"
        },
        "current": {
          "type": "integer"
        }
      },
      "required": ["size", "total", "totalPages", "current"]
    }
  },
  "required": ["code", "status", "data", "page"]
}
```

  </TabItem>
  <TabItem value="erorrs" label="Error">

```bash title="Error Example 1"
{
  "code": 500,
  "status": "Internal Server Error",
  "message": "An error occurred on the server"
}
```

```bash title="Error Example 2"
{
  "code": 400,
  "status": "Bad Request",
  "message": [
    {
      "message": "Email is required"
    }
  ]
}
```

```bash title="Error JSON Schema"
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "code": {
      "type": "integer"
    },
    "status": {
      "type": "string"
    },
    "message": {
      "type": "string"
    }
  },
  "required": ["code", "status", "message"]
}
```

  </TabItem>
</Tabs>
